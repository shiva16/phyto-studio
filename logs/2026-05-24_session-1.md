# Session 1 — 2026-05-24

## Goal
Resume and fix the Playwright TUI model test — all 5 models must pass.

## What was done

### 1. Resumed test with correct query
- Previous session left `/tmp/pt-test.js` with `QUERY='what is notes'` — the long query triggered agentic file-reading on 70B models
- Changed `QUERY` to `'hi'` then `'what is 1+1'`
- Changed `MODELS` to all 5: `['fast', 'quality', 'deepseek', 'qwen', 'coder']`

### 2. Test results before fixes
| Model | Status | Issue |
|---|---|---|
| fast | ✅ | Groq TPM rate limit error in tail but still passed |
| quality | ❌ | Agentic file-reading loop, timeout |
| deepseek | ❌ | Same file-reading loop, NIM too slow |
| qwen | ✅ | Answers directly, no file reading |
| coder | ✅ | Answers directly, no file reading |

### 3. Fix 1 — cap NIM history + tighten content truncation (commit eae495e)
- `maxHist` for deepseek/qwen/coder: was `PT.messages.length` (unbounded), now `8`
- `msgLimit` for NIM models: `800` chars (was `2000`)
- Result: quality ✅ fixed. deepseek ❌ still timing out at 134s.

### 4. Root cause identified
- NIM `meta/llama-3.3-70b-instruct` generates at ~15 tok/s
- `max_tokens=2048` → worst case ~136s generation time
- Model was generating a long response after reading commits, not stopping early
- Groq is fast (parallelized inference), NIM free tier is sequential and slow

### 5. Fix 2 — reduce NIM max_tokens 2048→512 (commit 1904018)
- Agentic responses are always short: `<gh>` action blocks or brief text
- 512 tokens at 15 tok/s = ~34s worst case, well within the 180s test timeout
- Applied to both Worker-proxied and direct NIM paths in `ptCallNim()`

## Pending
- Retest after GitHub Pages deploys fix 2 — confirming all 5 models pass
- Session log written before test rerun (hook compliance)

## Key facts
- Worker: `https://studio.carnivorousplants-in.workers.dev`
- Test script: `/tmp/pt-test.js` (QUERY=`'what is 1+1'`, MODELS=all 5)
- Run: `PHYTO_PAT=<redacted> PLAYWRIGHT_BROWSERS_PATH=/tmp/pw-browsers node /tmp/pt-test.js`
