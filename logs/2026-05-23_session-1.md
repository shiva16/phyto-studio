# Phyto Studio — Session 1 — 2026-05-23

## Work Done

### 1. Global session-log hook
- Created `~/.claude/hooks/check-session-log.sh`
- Added to `~/.claude/settings.json` as `PreToolUse` on `Bash(git commit*)`

### 2. Comprehensive Help Panel (`v2.html`)
Full `openHelpPanel(tab)` with 6 sections, search, keyboard nav — see session summary from earlier context.

### 3. CF Worker AI fix
**Root cause of "Worker error 405":** Worker URL saved as `studio.carnivorousplants-in.workers.dev` (missing `https://`). POST requests went to GitHub Pages which returns 405 for all POST.

**Fixes:**
- `saveDashSettings()`: auto-prepend `https://` if missing
- `worker/index.js`: removed `response_format`, added GROQ_API_KEY guard, `.text()` + parse error handling, restored `llama-3.1-8b-instant`
- `deploy-worker.yml`: simplified to just `wrangler deploy` — GROQ_API_KEY lives on CF Worker directly

### 4. Editor overhaul
- **Font picker**: custom dropdown, 24 Google Fonts, each rendered in its own typeface, searchable
- **Toolbar**: text-align L/C/R/J buttons (SVG icons), toolbar scrolls on small screens
- **Properties panel**: shadow section (enable/disable, color, blur, offsetX/Y), letter spacing, font preview
- **Bug fixes**: `backToDash()` clears `_pendingAiBrief`, catalogue filters `visible_from` date, `charSpacing` prop wired

### 5. Dashboard overhaul
- **Stats strip**: total / published / drafts / slides / brands
- **Featured row**: last 5 edited presentations, large horizontal scroll
- **Sort**: newest / oldest / A-Z / Z-A / published-first
- **Brand-gradient thumbnail fallback** with first-letter monogram
- **Slide count + relative time** on every card
- **Empty state** with CTA when brand filter returns nothing
- **Keyboard shortcuts**: N = new, / = search (cleaned up on editor open)
- **Greeting** with last-edited context
